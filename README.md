# Hybrid DQN：使用 GA 缓解 DQN 早期冷启动问题  
# Hybrid DQN: Using GA to Mitigate the Early Cold-Start Problem in DQN

## 1. 项目目标 / Project Objective

本项目通过对比 **Baseline DQN** 和 **Hybrid DQN（GA + DQN）** 在智能体避障到达目标过程中的训练表现与评估表现，判断混合 GA 的 DQN 强化学习方法是否可以缓解传统纯 DQN 在训练早期的 **cold start（冷启动）困境**。

This project compares the training and evaluation performance of **Baseline DQN** and **Hybrid DQN (GA + DQN)** in an obstacle-avoidance navigation task. The goal is to determine whether the GA-assisted DQN reinforcement learning method can mitigate the **cold-start problem** commonly observed in standard DQN during the early training stage.

核心问题是：

The core research question is:

> 使用 GA 生成的启发式控制器参数预先填充 DQN 的 replay buffer，是否能够减少早期无效碰撞经验，从而提高 DQN 的训练稳定性与样本效率？
>
> Can heuristic controller parameters generated by GA be used to pre-fill the DQN replay buffer, thereby reducing ineffective early collision experiences and improving the training stability and sample efficiency of DQN?

---

## 2. 为什么要解决冷启动问题？ / Why Solve the Cold-Start Problem?

### 2.1 冷启动困境的影响 / Impact of the Cold-Start Problem

DQN 在训练初期依赖 replay buffer 中的数据进行学习。如果前期大部分 episode 都是如下失败轨迹：

In the early training stage, DQN learns from the data stored in the replay buffer. If most early episodes are failed trajectories such as:

```text
状态 -> 随机动作 -> 很快碰撞 -> 负奖励
State -> Random action -> Quick collision -> Negative reward
```

那么 replay buffer 会被大量失败经验占据。此时网络学到的主要不是“如何到达目标”，而是大量短程失败轨迹。

Then the replay buffer will be dominated by failed experiences. In this case, the network mainly learns many short failure trajectories rather than learning how to reach the target.

在导航任务中，成功奖励通常是稀疏的。例如，只有智能体到达目标点时才会获得较大的正奖励。如果经验池中的成功样本过少，那么 DQN 在早期几乎看不到成功轨迹，自然也难以学习到“哪些状态-动作序列最终能够成功”。

In navigation tasks, success rewards are usually sparse. For example, the agent may only receive a large positive reward when it reaches the target. If there are too few successful samples in the replay buffer, DQN can hardly observe successful trajectories in the early stage. Therefore, it becomes difficult for the model to learn which state-action sequences can eventually lead to success.

这会带来以下问题：

This causes the following problems:

- 降低训练效率；  
  Reduced training efficiency;
- 延缓模型收敛速度；  
  Slower model convergence;
- 使 Q 值更新长期被负奖励主导；  
  Q-value updates are dominated by negative rewards for a long time;
- 使模型容易形成保守或错误策略，例如一直转圈、卡在某种动作偏好，或者学不到稳定的避障行为。  
  The model may develop conservative or incorrect policies, such as repeatedly turning in circles, being stuck in a biased action preference, or failing to learn stable obstacle-avoidance behavior.

---

## 3. 解决思路 / Proposed Solution

本项目使用 **GA（Genetic Algorithm，遗传算法）** 对低维启发式控制器参数进行搜索。其目标是找到一组较优的控制器参数，使智能体在一个生命周期内生成的动作轨迹能够获得尽可能高的 reward。

This project uses a **GA (Genetic Algorithm)** to search for low-dimensional heuristic controller parameters. The objective is to find a relatively good set of controller parameters so that the trajectory generated by the agent within an episode can obtain as high a reward as possible.

换句话说，GA 不是直接训练神经网络，而是先搜索一组能够产生较好行为的启发式控制器参数。然后使用这组参数控制智能体运行，并将生成的 transition 存入 DQN 的 replay buffer 中。

In other words, GA does not directly train the neural network. Instead, it first searches for a set of heuristic controller parameters that can generate better behavior. These parameters are then used to control the agent, and the generated transitions are stored in the DQN replay buffer.

这样可以用 GA 产生的较高质量轨迹替代纯 DQN 早期的随机探索轨迹，从而缓解冷启动问题。

This allows the higher-quality trajectories generated by GA to replace the random exploration trajectories used by standard DQN in the early stage, thereby mitigating the cold-start problem.

---

### 3.1 GA 启发式控制器参数 / GA Heuristic Controller Parameters

GA 搜索得到的参数向量用于控制智能体如何在目标吸引和障碍物避让之间进行权衡。

The parameter vector found by GA controls how the agent balances target attraction and obstacle avoidance.

| 参数 / Parameter | 名称 / Name | 含义 / Meaning |
|---|---|---|
| `params[0]` | `w_target` | 目标方向权重。控制智能体朝目标方向进行偏航角修正。<br>Target-direction weight. It controls how strongly the agent corrects its yaw angle toward the target. |
| `params[1]` | `w_side` | 左右障碍物危险差异权重。当 `left_danger > right_danger` 时，说明左侧更危险，智能体应倾向向右转。<br>Left-right obstacle danger difference weight. When `left_danger > right_danger`, the left side is more dangerous, so the agent should tend to turn right. |
| `params[2]` | `w_front` | 正前方危险权重。当前方射线距离很短时，增强避障行为。<br>Front danger weight. When the front ray distance is very short, the obstacle-avoidance behavior is strengthened. |
| `params[3]` | `w_front_group` | 前方一组射线危险权重。不只看正中间一根 ray，而是观察 `rays[4:7]` 的局部危险情况。<br>Front-ray group danger weight. Instead of only considering the middle ray, it observes the local danger represented by `rays[4:7]`. |
| `params[4]` | `bias` | 转向偏置。<br>Steering bias. |
| `params[5]` | `threshold_small` | 小转向阈值。<br>Small steering threshold. |
| `params[6]` | `threshold_large` | 大转向阈值。<br>Large steering threshold. |

---

### 3.2 使用 GA 控制器参数填充 DQN Replay Buffer / Filling the DQN Replay Buffer with GA Controller Parameters


GA 得到 `best_params` 后，将该参数组放入环境中控制智能体行动。每一步产生的经验都会被存入 DQN 的 replay buffer。

After GA obtains `best_params`, this parameter set is placed into the environment to control the agent's behavior. The experience generated at each step is stored in the DQN replay buffer.

整体流程如下：

The overall process is as follows:

```text
GA 得到 best_params
GA obtains best_params
        ↓
每一步读取当前 state
Read the current state at each step
        ↓
heuristic_action(state, best_params) 计算动作
heuristic_action(state, best_params) computes the action
        ↓
环境执行动作
The environment executes the action
        ↓
得到 reward、next_state、done
Obtain reward, next_state, and done
        ↓
把这一步 transition 存入 replay buffer
Store this transition in the replay buffer
        ↓
如果 done，就 reset 环境，开始下一条轨迹
If done, reset the environment and start the next trajectory
```

其中，单步经验可以表示为：

A single-step experience can be represented as:

```text
transition = (state, action, reward, next_state, done)
```

通过这个流程，Hybrid DQN 用 GA 生成的启发式轨迹替代纯 DQN 前期对 replay buffer 的随机填充策略。这样可以提升 replay buffer 的前期样本质量，从而提升训练效率并加快模型收敛。

Through this process, Hybrid DQN replaces the random replay-buffer filling strategy used by standard DQN in the early stage with heuristic trajectories generated by GA. This improves the early sample quality of the replay buffer, thereby improving training efficiency and accelerating model convergence.

---

## 5. 评价指标设计 / Evaluation Metric Design

本项目分别对 **Baseline DQN** 和 **Hybrid DQN** 各训练三次。每次训练结束后，将训练得到的模型在环境中运行 100 轮，并统计评估指标。

In this project, **Baseline DQN** and **Hybrid DQN** are each trained three times. After each training run, the trained model is evaluated in the environment for 100 episodes, and the evaluation metrics are recorded.

评价指标分为两类：

The evaluation metrics are divided into two categories:

1. **Training Stats**：主要用于判断方法是否能够缓解训练早期的冷启动问题；  
   **Training Stats**: mainly used to determine whether the method can mitigate the cold-start problem during early training;
2. **Evaluation Stats**：主要用于评估最终模型的整体性能。  
   **Evaluation Stats**: mainly used to evaluate the overall performance of the final model.

---

## 6. Training Stats：冷启动缓解效果  
## 6.1 Training Stats: Cold-Start Mitigation Effect

### 6.2 Training Early Collision Rate

该指标统计模型在前 500 个 training step 中发生碰撞的占比。这里的 training step 指模型进行一次反向传播更新。

This metric measures the proportion of collisions that occur during the first 500 training steps. Here, one training step refers to one backpropagation update of the model.

#### 15 obstacles ####

| 方法 / Method | Early Collision Rate |
|---|---:|
| Baseline DQN | 0.983 |
| Hybrid DQN | 0.717 |

#### 5 obstacles ####

| 方法 / Method | Early Collision Rate |
|---|---------------------:|
| Baseline DQN |                0.893 |
| Hybrid DQN |                0.387 |

从结果可以看出，Hybrid DQN 的早期碰撞率明显低于 Baseline DQN，说明 GA 预填充经验池能够减少早期随机探索导致的无效碰撞经验。

The results show that the early collision rate of Hybrid DQN is clearly lower than that of Baseline DQN. This indicates that GA-based replay-buffer pre-filling can reduce ineffective collision experiences caused by early random exploration.

### 6.3 Average Return over Training Steps

该指标表示在训练过程中，每 2000 次 training step 对当前模型进行一次评估，并运行 50 轮环境交互，统计这 50 轮的平均回报。

This metric means that during training, the current model is evaluated every 2000 training steps. For each evaluation, the model runs 50 environment episodes, and the average return over these 50 episodes is recorded.

该指标用于观察模型在训练过程中的性能提升趋势。

This metric is used to observe the performance improvement trend of the model during training.

### 6.4 Convergence Step

`Convergence Step` 用来衡量模型首次达到稳定性能水平的速度，即模型多快达到一个可接受的性能水平。

`Convergence Step` measures how quickly the model first reaches a stable performance level, that is, how fast the model reaches an acceptable level of performance.

本项目中，convergence 的定义是：

In this project, convergence is defined as follows:

> 当连续 `window_size` 次评估中，`Avg Return` 都大于或等于设定的 `threshold` 时，认为模型第一次收敛。
>
> When `Avg Return` is greater than or equal to the predefined `threshold` for `window_size` consecutive evaluations, the model is considered to have converged for the first time.

#### 15 obstacles ####

| 方法 / Method | 平均 Convergence Step / Average Convergence Step | 解释 / Explanation |
|---|---:|---|
| Baseline DQN | 79333 | 早期容易被负奖励主导，导致模型真正学会有效避障所需时间较长。<br>In the early stage, learning is easily dominated by negative rewards, so it takes longer for the model to learn effective obstacle avoidance. |
| Hybrid DQN | 32666 | 早期不再被大量负奖励主导，模型可以更早学习到有效避障行为。<br>The early stage is less dominated by negative rewards, allowing the model to learn effective obstacle-avoidance behavior earlier. |

#### 5 obstacles #####

| 方法 / Method | 平均 Convergence Step / Average Convergence Step | 解释 / Explanation |
|---|-----------------------------------------------:|---|
| Baseline DQN |                                          36667 | 早期容易被负奖励主导，导致模型真正学会有效避障所需时间较长。<br>In the early stage, learning is easily dominated by negative rewards, so it takes longer for the model to learn effective obstacle avoidance. |
| Hybrid DQN |                                         106667 | 早期不再被大量负奖励主导，模型可以更早学习到有效避障行为。<br>The early stage is less dominated by negative rewards, allowing the model to learn effective obstacle-avoidance behavior earlier. |


从结果看，Hybrid DQN 的平均收敛步数明显少于 Baseline DQN，说明 GA 辅助经验填充可以提升早期训练效率。

The results show that the average convergence step of Hybrid DQN is much smaller than that of Baseline DQN. This indicates that GA-assisted experience filling can improve early-stage training efficiency.

---

## 7. Evaluation Stats：最终模型性能  / Evaluation Stats: Final Model Performance
 

### 7.1 Average Return over Evaluation


#### 15 obstacles  ####

| 方法 / Method | Average Return |
|---|---:|
| Baseline DQN | 36.813 |
| Hybrid DQN | 50.515 |

#### 5 obstacles  ####

| 方法 / Method | Average Return |
|---|---------------:|
| Baseline DQN |        106.649 |
| Hybrid DQN |        106.964 |

Hybrid DQN 的平均回报略高于 Baseline DQN，说明其最终模型在整体表现上有一定提升，但不明显。

The average return of Hybrid DQN is slightly higher than that of Baseline DQN, indicating that the final model has improved overall performance to some extent but the improvement is not very significant.

### 7.2 Success Rate over Evaluation

#### 15 obstacles ####
| 方法 / Method | Success Rate |
|---|---:|
| Baseline DQN | 0.34 |
| Hybrid DQN | 0.41 |

#### 5 obstacles ####
| 方法 / Method | Success Rate |
|---|-------------:|
| Baseline DQN |         0.71 |
| Hybrid DQN |         0.72 |

Hybrid DQN 的成功率略高于 Baseline DQN，但提升幅度并不非常显著。

The success rate of Hybrid DQN is slightly higher than that of Baseline DQN, but the improvement is not very significant.

### 7.3 Path Efficiency

Path Efficiency 的计算公式为：

The formula for Path Efficiency is:

```text
Path Efficiency = actual path length / optimal path length
```

其中，`optimal path length` 是智能体初始位置到目标点之间的欧氏距离。

Here, `optimal path length` refers to the Euclidean distance between the agent's initial position and the target point.


#### 15 obstacles ####
| 方法 / Method | Path Efficiency |
|---|---:|
| Baseline DQN | 1.0708 |
| Hybrid DQN | 1.0685 |

#### 5 obstacles ####
| 方法 / Method | Path Efficiency |
|---|----------------:|
| Baseline DQN |          1.0389 |
| Hybrid DQN |          1.0385 |

两者路径效率非常接近，说明 Hybrid DQN 在最终路径质量上相比 Baseline DQN 并没有产生明显差距。

The path efficiencies of the two methods are very close. This indicates that Hybrid DQN does not produce a clear difference in final path quality compared with Baseline DQN.

---

## 8. 总结 / Conclusion

总体来看，Hybrid DQN 在最终模型 performance 上相比 Baseline DQN 没有非常大的提升，但它能够明显缓解 Baseline DQN 在训练早期的 cold start 问题。

Overall, Hybrid DQN does not greatly improve the final model performance compared with Baseline DQN. However, it can clearly mitigate the cold-start problem of Baseline DQN during early training.

Hybrid DQN 的核心优势在于：

The core advantages of Hybrid DQN are:

- 让 DQN 早期少收集垃圾碰撞经验；  
  It reduces the amount of low-quality collision experience collected by DQN in the early stage;
- 让 replay buffer 中出现更多有学习价值的安全轨迹；  
  It allows the replay buffer to contain more safe trajectories with learning value;
- 减少早期负奖励对 Q 值更新的主导；  
  It reduces the dominance of negative rewards in early Q-value updates;
- 提高训练初期的稳定性；  
  It improves stability during the initial training stage;
- 提升样本效率，并加快模型达到稳定性能的速度。  
  It improves sample efficiency and accelerates the process of reaching stable performance.

因此，本项目的结论是：

Therefore, the conclusion of this project is:

> GA 预训练或 GA 经验填充不一定显著提升最终模型上限，但可以有效改善 DQN 的早期训练质量，尤其适合用于缓解强化学习导航任务中的冷启动问题。
>
> GA pre-training or GA-based experience filling does not necessarily significantly improve the upper bound of the final model performance, but it can effectively improve the early training quality of DQN. It is especially suitable for mitigating the cold-start problem in reinforcement learning navigation tasks.
